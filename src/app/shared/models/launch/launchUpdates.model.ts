export interface ILaunchUpdatesModel {
        id:number,
        profile_image: string | null,
        comment: string | null,
        info_url: string | null,
        created_by: string,
        created_on: string
}
